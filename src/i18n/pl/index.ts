import en from '../en';

// Placeholder: the Polish copy lands in the translation commit. Until then /pl/
// renders the English strings so the routing and head work can be verified on
// their own.
const pl = en satisfies typeof en;

export default pl;
