---
import { Terminal as TerminalReact } from '@virtuslab/visdom-ui';

interface Props {
  title?: string;
}

const { title = 'Terminal' } = Astro.props;
---
<TerminalReact title={title} client:load>
  <slot />
</TerminalReact>
