---
// Polish home page. Same sections as /, copy resolved from the locale in the
// URL, so this file never needs touching when a section changes.
import Layout from '../../layouts/Layout.astro';
import HomeSections from '../../components/HomeSections.astro';
import '../../styles/global.css';
---
<Layout>
  <HomeSections />
</Layout>
