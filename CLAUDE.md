## Style

- Never use em dashes (—) in customer-facing copy, code comments, commit messages, or any written output. Use a period, a comma, a colon, or parentheses instead. Applies to en dashes (–) used as separators too.
